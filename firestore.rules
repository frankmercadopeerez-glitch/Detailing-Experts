rules_version = '2';
service cloud.firestore {
 match /databases/{database}/documents {
  // All data access is mediated by the authenticated server API.
  // No client can edit ownership, payment status or grant itself permissions.
  match /{document=**} { allow read, write: if false; }
 }
}
